<lccdd>
  <define>
    <include ref="ip6/definitions.xml" /> 
    <include ref="compact/definitions.xml" />
    <include ref="compact/configuration_default.xml"/>
  </define>

  <includes>
    <gdmlFile ref="compact/elements.xml"/>
    <gdmlFile ref="compact/materials.xml"/>
  </includes>

  <limits>
  </limits>

  <regions>
  </regions>

  <display>
  <include ref="compact/colors.xml" />
  </display>
  
  <comment> Including the central beampipe </comment>
  <include ref="ip6/central_beampipe.xml" />

  <comment> Including the pEndcap insert </comment>
  <include ref="compact/pECalInsert.xml"/>
  <include ref="compact/pHCalInsert.xml"/>
  <!-- <include ref="compact/leakage_detector.xml"/> -->

  <comment> Including the pEndcap homogeneous </comment>
  <include ref="compact/ecal_forward_homogeneous.xml"/>

  <comment> Including the pHCal </comment>
  <comment> Must be included after pHCalInsert </comment>
  <comment> Change to compact/hcal_ECCE.xml for ECCE pHCal</comment>
  <include ref="compact/hcal_ATHENA.xml"/>

  <detectors>
  </detectors>

  <readouts>
  </readouts>

  <plugins>
  </plugins>

  <fields>
  </fields>

</lccdd>
